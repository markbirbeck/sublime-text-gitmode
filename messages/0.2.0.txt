Version 0.2.0 Release Notes

`GitMode` should now be a lot better at working out which directory to use when viewing a file in a project. This makes things a lot easier when using multiple folders in a project.
